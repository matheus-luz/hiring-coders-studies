const { resolve } = require('path'); 

module.exports = {
  config: resolve(__dirname, 'src', 'config',
  'database.js'),
  'model-paths': resolve(__dirname, 'src', 'app', 'models'),
  'migrations-paths': resolve(__dirname, 'src', 'databse', 'migrations'),
  'seeders-paths': resolve(__dirname, 'src', 'database', 'seeds')
}